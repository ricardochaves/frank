[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "frank"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["requests"]

[project.scripts]
frank = "frank.cli:main"
